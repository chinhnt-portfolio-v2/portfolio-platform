/**
 * Auth session package — Session entity and SessionRepository live here ONLY.
 * No other package may import Session directly (architectural boundary).
 */
package dev.chinh.portfolio.auth.session;
